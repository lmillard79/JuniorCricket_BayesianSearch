[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "junior-cricket"
version = "0.1.0"
description = "Bayesian MARCEL lineup optimiser for BNJCA junior cricket"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
